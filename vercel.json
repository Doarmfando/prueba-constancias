{
   "_comment": "Solo sirve como nota temporal, quítalo antes del deploy",
  "version": 2,
  "framework": "other",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
